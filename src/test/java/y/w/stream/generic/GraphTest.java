package y.w.stream.generic;

import org.junit.Test;

public class GraphTest {
    @Test
    public void graphTest() {
        // Shape consists of zero or more inputs and zero or more outputs.
        // ClosedShape - all inlets and outlets are connected.

        // SourceShape, SinkShape, FlowShape
        // UniformFanInShape
        // UniformFanOutShape

        // Source.combine(source1, source2, ...)
        // Sink.combine(...)
    }
}
